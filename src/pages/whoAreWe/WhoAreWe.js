import React from "react";

import style from './whoAreWe.module.scss';

const WhoAreWe = () => {
    return (
        <div className={`${style['page']}`}>
            Who Are We
            {/* Main Section */}
            {/* set of sections to describe who we are and what we do */}
            {/* our team section (every one in the team and his role and what he do) */}
        </div>
    );
};

export default WhoAreWe;