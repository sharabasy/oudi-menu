import React from "react";

import style from './ourServices.module.scss';

const OurServices = () => {
    return (
        <div className={`${style['page']}`}>
            Our Services
            {/* Main section */}
            {/* a shape or set of cards to describe our services */}
        </div>
    );
};

export default OurServices;